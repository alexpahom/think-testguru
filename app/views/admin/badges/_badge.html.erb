<tr>
  <td><%= badge.name %></td>
  <td><%= badge.description %></td>
  <td><%= badge.rule_template.text %></td>
  <td><%= link_to t('admin.actions.show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('admin.actions.edit'), edit_admin_badge_path(badge) %></td>
  <td><%= link_to t('admin.actions.delete'),
                  admin_badge_path(badge),
                  method: :delete,
                  data: { confirm: 'Delete?' } %></td>
</tr>
