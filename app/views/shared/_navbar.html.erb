<nav>
  <h3><a class="logo-link" href="<%= root_path %>">Test GURU</a></h3>
  <div>
    <% if user_signed_in? %>
      <p class="nav user">
        <span><%= t('general.welcome', first_name: current_user.first_name)%></span> |
        <%= link_to t('general.logout'), destroy_user_session_path, method: :delete %>
      </p>
    <% else %>
      <%= link_to t('general.login'), new_user_session_path %>
      <%= link_to t('general.signup'), new_user_registration_path %>
    <% end %>
  </div>
</nav>
